# FitContentAI 웹 개발 PRD (Product Requirements Document)

**문서 버전:** 2.0  
**작성일:** 2025-01-23  
**타겟:** 프론트엔드/백엔드 개발자  
**목적:** 홈페이지 구현을 위한 상세 개발 명세서

---

## 📋 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [기술 스택](#2-기술-스택)
3. [사이트 구조](#3-사이트-구조)
4. [화면별 상세 명세](#4-화면별-상세-명세)
5. [핵심 기능: AI 블로그 생성기](#5-핵심-기능-ai-블로그-생성기)
6. [핵심 기능: 이미지 생성 시스템](#6-핵심-기능-이미지-생성-시스템)
7. [API 명세](#7-api-명세)
8. [데이터베이스 스키마](#8-데이터베이스-스키마)
9. [컴포넌트 구조](#9-컴포넌트-구조)
10. [개발 우선순위](#10-개발-우선순위)

---

## 1. 프로젝트 개요

### 1.1 제품명
**FitContentAI** - 피트니스 블로거 성장 플랫폼

### 1.2 핵심 가치
AI 기반 콘텐츠 자동 생성 + 커뮤니티 품앗이 = 블로그 빠른 성장

### 1.3 주요 기능
1. **AI 블로그 생성기** (페르소나 기반)
2. **AI 이미지 생성** (나노바나나 통합)
3. **커뮤니티 품앗이 시스템**
4. **SEO 분석 도구**
5. **블로거 순위 & 경쟁사 분석**

---

## 2. 기술 스택

### 2.1 Frontend

```yaml
Framework: Next.js 14 (App Router)
Language: TypeScript 5
UI Library: React 18

Styling:
  - TailwindCSS 3.4
  - Shadcn/ui (컴포넌트)
  - Framer Motion (애니메이션)

State Management:
  - Zustand (글로벌 상태)
  - React Query (서버 상태/캐싱)

Form:
  - React Hook Form
  - Zod (스키마 검증)

Charts:
  - Recharts
  - D3.js (고급 시각화)

Icons:
  - Lucide React

Editor:
  - Tiptap (리치 에디터)
```

### 2.2 Backend

```yaml
API Server:
  - Node.js + Express.js
  - TypeScript
  - JWT 인증

AI Service:
  - Python + FastAPI
  - OpenAI API (GPT-4)
  - 나노바나나 API (이미지 생성)

Database:
  - PostgreSQL 15 (Prisma ORM)
  - Redis (캐싱/세션)

Storage:
  - AWS S3 / Cloudflare R2
  - 이미지 최적화 (Sharp)

Queue:
  - BullMQ (비동기 작업)
```

### 2.3 Infrastructure

```yaml
Hosting:
  - Vercel (Frontend)
  - AWS EC2 / DigitalOcean (Backend)
  - Cloudflare (CDN)

Monitoring:
  - Sentry (에러 추적)
  - Vercel Analytics
  - PostHog (사용자 행동)

CI/CD:
  - GitHub Actions
  - Docker
```

---

## 3. 사이트 구조

### 3.1 페이지 맵

```
/ (홈)
│
├─ /auth
│  ├─ /login (로그인)
│  ├─ /signup (회원가입)
│  └─ /reset-password (비밀번호 찾기)
│
├─ /dashboard (대시보드) 🔒
│  └─ 메인 허브
│
├─ /ai-writer (AI 글쓰기) 🔒
│  ├─ /new (새 글 생성)
│  ├─ /edit/:id (수정)
│  └─ /history (생성 이력)
│
├─ /images (이미지 생성) 🔒
│  ├─ /generate (새 이미지)
│  └─ /library (이미지 라이브러리)
│
├─ /keywords (키워드 분석)
│  ├─ /search (검색량 조회)
│  ├─ /trends (트렌드)
│  └─ /recommendations (추천)
│
├─ /community (커뮤니티) 🔒
│  ├─ /challenges (챌린지 그룹)
│  ├─ /missions (일일 미션)
│  ├─ /leaderboard (리더보드)
│  └─ /points (포인트 상점)
│
├─ /analytics (분석) 🔒
│  ├─ /blog (내 블로그 분석)
│  ├─ /competitor (경쟁사 분석)
│  └─ /rankings (블로거 순위)
│
├─ /publish (발행) 🔒
│  ├─ /quick (빠른 복사)
│  ├─ /schedule (예약 발행)
│  └─ /history (발행 이력)
│
├─ /settings (설정) 🔒
│  ├─ /profile (프로필)
│  ├─ /blog (블로그 연동)
│  ├─ /subscription (구독 관리)
│  └─ /preferences (환경설정)
│
└─ /pricing (요금제)

🔒 = 로그인 필요
```

### 3.2 라우팅 구조

```typescript
// app/layout.tsx
export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="ko">
      <body className={pretendard.variable}>
        <Providers>
          <Header />
          <main>{children}</main>
          <Footer />
        </Providers>
      </body>
    </html>
  )
}

// app/(auth)/layout.tsx - 인증 페이지 레이아웃
export default function AuthLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gradient-to-br from-primary-50 to-info-50">
      <div className="w-full max-w-md">
        {children}
      </div>
    </div>
  )
}

// app/(dashboard)/layout.tsx - 대시보드 레이아웃
export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <div className="flex min-h-screen">
      <Sidebar />
      <div className="flex-1">
        <DashboardHeader />
        <main className="p-6">
          {children}
        </main>
      </div>
    </div>
  )
}
```

---

## 4. 화면별 상세 명세

### 4.1 홈페이지 (/)

#### 레이아웃
```
┌─────────────────────────────────────────────┐
│ Header (고정)                               │
│ [Logo] [기능] [요금제] [로그인] [시작하기] │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ Hero Section                                │
│                                             │
│ [왼쪽: 카피 + CTA]                         │
│ AI와 커뮤니티로                            │
│ 블로그 성장을 가속화하세요                 │
│                                             │
│ [무료로 시작하기 버튼]                     │
│ ✓ 신용카드 필요 없음                       │
│                                             │
│ [오른쪽: 일러스트/스크린샷]                │
│                                             │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ Social Proof                                │
│ "이미 1,234명의 블로거가 함께 성장하고     │
│  있습니다" + 로고 5개                       │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ Features Section                            │
│                                             │
│ [3개 컬럼 그리드]                          │
│                                             │
│ 🤖 AI 전문가급 글쓰기                      │
│ 2분이면 2000자 완성                        │
│                                             │
│ 🤝 커뮤니티 품앗이                         │
│ 함께 성장하는 블로거들                     │
│                                             │
│ 📊 데이터 기반 성장                        │
│ SEO 분석 + 경쟁사 분석                     │
│                                             │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ How It Works                                │
│                                             │
│ [4단계 프로세스]                           │
│                                             │
│ 1. 페르소나 선택                           │
│ 2. AI가 글 생성 (2분)                      │
│ 3. 커뮤니티 부스트                         │
│ 4. 블로그 폭발 성장                        │
│                                             │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ Testimonials (증언)                         │
│                                             │
│ [3개 카드 슬라이더]                        │
│ 실제 사용자 후기 + 성과                    │
│                                             │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ Pricing (요금제)                            │
│                                             │
│ [3개 티어 비교 표]                         │
│ Free / Pro / Enterprise                     │
│                                             │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ CTA Section                                 │
│                                             │
│ 지금 시작해서                               │
│ 3개월 안에 블로그를 성장시키세요           │
│                                             │
│ [무료로 시작하기 버튼 (큰)]                │
│                                             │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ Footer                                      │
│ [회사 정보] [링크] [SNS] [이용약관]        │
└─────────────────────────────────────────────┘
```

#### 컴포넌트 명세

```typescript
// app/page.tsx
export default function HomePage() {
  return (
    <>
      <HeroSection />
      <SocialProof />
      <FeaturesSection />
      <HowItWorks />
      <Testimonials />
      <PricingSection />
      <CTASection />
    </>
  )
}

// components/home/HeroSection.tsx
export function HeroSection() {
  return (
    <section className="container mx-auto px-4 py-20">
      <div className="grid lg:grid-cols-2 gap-12 items-center">
        {/* 왼쪽: 카피 */}
        <div className="space-y-6">
          <h1 className="text-5xl font-bold leading-tight">
            AI와 커뮤니티로
            <br />
            <span className="text-primary-500">블로그 성장</span>을
            <br />
            가속화하세요
          </h1>
          
          <p className="text-xl text-gray-600">
            전문가급 글쓰기부터 초기 트래픽까지,
            <br />
            3개월 안에 월 방문자 1만 달성
          </p>
          
          <div className="flex gap-4">
            <Button size="lg" className="text-lg px-8 py-6">
              무료로 시작하기 →
            </Button>
            <Button variant="outline" size="lg">
              데모 보기
            </Button>
          </div>
          
          <div className="flex items-center gap-2 text-sm text-gray-500">
            <Check className="w-4 h-4 text-success-500" />
            <span>신용카드 필요 없음</span>
            <Check className="w-4 h-4 text-success-500" />
            <span>언제든 취소 가능</span>
          </div>
        </div>
        
        {/* 오른쪽: 비주얼 */}
        <div className="relative">
          <Image
            src="/images/hero-dashboard.png"
            alt="FitContentAI 대시보드"
            width={600}
            height={400}
            className="rounded-2xl shadow-2xl"
          />
          
          {/* 플로팅 카드들 */}
          <FloatingCard
            delay={0}
            className="absolute -left-8 top-20"
          >
            <div className="flex items-center gap-2">
              <Zap className="text-info-500" />
              <span className="text-sm">AI 글 생성 완료!</span>
            </div>
          </FloatingCard>
          
          <FloatingCard
            delay={0.5}
            className="absolute -right-4 top-40"
          >
            <div className="flex items-center gap-2">
              <TrendingUp className="text-success-500" />
              <span className="text-sm">방문자 +150%</span>
            </div>
          </FloatingCard>
        </div>
      </div>
    </section>
  )
}

// 플로팅 애니메이션 카드
function FloatingCard({ 
  children, 
  delay, 
  className 
}: {
  children: React.ReactNode
  delay: number
  className?: string
}) {
  return (
    <motion.div
      initial={{ y: 0 }}
      animate={{ y: [-10, 10, -10] }}
      transition={{
        duration: 3,
        repeat: Infinity,
        delay: delay,
      }}
      className={`
        bg-white rounded-lg shadow-lg p-4
        ${className}
      `}
    >
      {children}
    </motion.div>
  )
}
```

---

### 4.2 대시보드 (/dashboard)

#### 레이아웃
```
┌─────────────────────────────────────────────┐
│ Sidebar (왼쪽 고정 - 240px)                 │
│                                             │
│ [Logo]                                      │
│                                             │
│ 📊 대시보드                                 │
│ ✍️  AI 글쓰기                               │
│ 🖼️  이미지 생성                             │
│ 🔍 키워드 분석                              │
│ 🤝 커뮤니티                                 │
│ 📈 성과 분석                                │
│ 🚀 발행 관리                                │
│                                             │
│ ─────────────                               │
│                                             │
│ ⚙️  설정                                    │
│ 💰 포인트: 850P                             │
│                                             │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ Header (상단 고정)                          │
│                                             │
│ [경로] 대시보드                             │
│                                             │
│ [검색] [알림] [프로필 ▼]                   │
│                                             │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ Main Content Area                           │
│                                             │
│ ┌─ Hero Card ─────────────────────────┐   │
│ │ 👋 민수님, 안녕하세요!               │   │
│ │                                      │   │
│ │ 🔥 7일 연속 | 💰 850P | Lv.12      │   │
│ │                                      │   │
│ │ [빠른 액션 버튼 3개]                │   │
│ │ [AI 글쓰기] [미션하기] [포인트샵]  │   │
│ └──────────────────────────────────────┘   │
│                                             │
│ ┌─ 주요 지표 (3개 카드) ──────────────┐   │
│ │                                      │   │
│ │ 📊 월 방문자      📝 이번달 발행    │   │
│ │    3,200 (+15%)      12개           │   │
│ │                                      │   │
│ │              📈 블로그 지수          │   │
│ │                 준최 2 (↑1)         │   │
│ │                                      │   │
│ └──────────────────────────────────────┘   │
│                                             │
│ ┌─ 2단 레이아웃 ──────────────────────┐   │
│ │                                      │   │
│ │ [70% 왼쪽]        [30% 오른쪽]      │   │
│ │                                      │   │
│ │ 오늘의 미션       내 그룹            │   │
│ │ ✅ 글 읽기 3/3    8명 중 2위        │   │
│ │ ✅ 댓글 3/3                          │   │
│ │ ☐ 좋아요 2/5      리더보드          │   │
│ │ ☐ 글 발행 0/1     1. 홈트왕         │   │
│ │                   2. 나              │   │
│ │ [계속하기 →]                         │   │
│ │                   알림               │   │
│ │ 최근 활동         🔔 새 댓글 3개   │   │
│ │ • AI 글 생성                         │   │
│ │ • 커뮤니티 활동                      │   │
│ │                                      │   │
│ └──────────────────────────────────────┘   │
│                                             │
│ ┌─ 이번 주 성과 차트 ─────────────────┐   │
│ │                                      │   │
│ │ [풀 위드 라인 차트]                 │   │
│ │ 방문자 추이 ▁▂▃▅▆█▆                │   │
│ │                                      │   │
│ └──────────────────────────────────────┘   │
│                                             │
└─────────────────────────────────────────────┘
```

#### 컴포넌트 구조

```typescript
// app/(dashboard)/dashboard/page.tsx
export default async function DashboardPage() {
  const user = await getCurrentUser()
  const stats = await getUserStats(user.id)
  const missions = await getTodayMissions(user.id)
  const group = await getUserGroup(user.id)
  
  return (
    <div className="space-y-6">
      {/* Hero */}
      <HeroCard user={user} stats={stats} />
      
      {/* 주요 지표 */}
      <StatsGrid stats={stats} />
      
      {/* 2단 레이아웃 */}
      <div className="grid lg:grid-cols-[70%_30%] gap-6">
        <div className="space-y-6">
          <TodayMissions missions={missions} />
          <RecentActivity userId={user.id} />
        </div>
        
        <div className="space-y-6">
          <MyGroup group={group} />
          <Leaderboard groupId={group?.id} />
          <NotificationsFeed userId={user.id} />
        </div>
      </div>
      
      {/* 차트 */}
      <WeeklyPerformance userId={user.id} />
    </div>
  )
}

// components/dashboard/HeroCard.tsx
export function HeroCard({ user, stats }) {
  return (
    <Card className="bg-gradient-to-br from-primary-50 to-info-50 border-none">
      <CardContent className="p-6">
        <div className="flex items-center justify-between">
          <div>
            <h2 className="text-2xl font-bold mb-2">
              👋 {user.nickname}님, 안녕하세요!
            </h2>
            
            <div className="flex items-center gap-4 text-sm">
              <Badge variant="secondary" className="gap-1">
                🔥 {stats.streak}일 연속
              </Badge>
              <Badge variant="secondary" className="gap-1">
                💰 {stats.points}P
              </Badge>
              <Badge variant="secondary" className="gap-1">
                Lv.{stats.level}
              </Badge>
            </div>
          </div>
          
          <div className="flex gap-3">
            <Link href="/ai-writer/new">
              <Button size="lg" className="gap-2">
                <Sparkles className="w-4 h-4" />
                AI 글쓰기
              </Button>
            </Link>
            
            <Link href="/community/missions">
              <Button variant="outline" size="lg">
                미션하기
              </Button>
            </Link>
            
            <Link href="/community/points">
              <Button variant="outline" size="lg">
                포인트샵
              </Button>
            </Link>
          </div>
        </div>
      </CardContent>
    </Card>
  )
}

// components/dashboard/StatsGrid.tsx
export function StatsGrid({ stats }) {
  return (
    <div className="grid md:grid-cols-3 gap-4">
      <StatCard
        icon={<Users className="w-6 h-6 text-primary-500" />}
        label="월 방문자"
        value={stats.monthlyVisitors.toLocaleString()}
        change={stats.visitorsChange}
        trend="up"
      />
      
      <StatCard
        icon={<FileText className="w-6 h-6 text-info-500" />}
        label="이번 달 발행"
        value={`${stats.monthlyPosts}개`}
        sublabel={`AI 생성 ${stats.aiPosts}개`}
      />
      
      <StatCard
        icon={<TrendingUp className="w-6 h-6 text-success-500" />}
        label="블로그 지수"
        value={stats.blogIndex}
        change={stats.indexChange}
        badge={stats.indexLevel}
      />
    </div>
  )
}

function StatCard({ icon, label, value, change, trend, badge, sublabel }) {
  return (
    <Card>
      <CardContent className="p-6">
        <div className="flex items-center justify-between mb-4">
          <div className="p-2 bg-gray-50 rounded-lg">
            {icon}
          </div>
          
          {change && (
            <Badge 
              variant={trend === 'up' ? 'success' : 'destructive'}
              className="gap-1"
            >
              {trend === 'up' ? '↑' : '↓'} {change}
            </Badge>
          )}
          
          {badge && (
            <Badge variant="secondary">
              {badge}
            </Badge>
          )}
        </div>
        
        <div>
          <p className="text-sm text-gray-500 mb-1">{label}</p>
          <p className="text-2xl font-bold">{value}</p>
          {sublabel && (
            <p className="text-xs text-gray-400 mt-1">{sublabel}</p>
          )}
        </div>
      </CardContent>
    </Card>
  )
}
```

---

## 5. 핵심 기능: AI 블로그 생성기

### 5.1 페르소나 시스템 설계

#### 페르소나 카테고리 & 프로필

```typescript
// types/persona.ts

export interface Persona {
  id: string
  name: string
  category: PersonaCategory
  description: string
  expertise: string[]
  tone: ToneStyle
  writingStyle: WritingStyle
  targetAudience: string
  experienceYears: number
  credentials: string[]
  sampleIntro: string
  avatar: string
}

export type PersonaCategory = 
  | 'trainer'      // 트레이너
  | 'owner'        // 관장/센터장
  | 'medical'      // 의료 전문가
  | 'instructor'   // 강사
  | 'counselor'    // FC (Fitness Counselor - 피트니스 상담사)

export type ToneStyle = 
  | 'professional' // 전문적
  | 'friendly'     // 친근한
  | 'motivational' // 동기부여
  | 'educational'  // 교육적
  | 'casual'       // 캐주얼

export type WritingStyle = 
  | 'detailed'     // 상세한
  | 'concise'      // 간결한
  | 'storytelling' // 스토리텔링
  | 'datadriven'   // 데이터 기반

// 페르소나 데이터
export const PERSONAS: Persona[] = [
  // 1. 10년차 관장님
  {
    id: 'gym-owner-10y',
    name: '10년차 헬스장 관장',
    category: 'owner',
    description: '10년 경력의 헬스장 운영자. 수백 명의 회원을 관리하며 얻은 실전 노하우 보유',
    expertise: [
      '헬스장 운영',
      '회원 관리',
      '운동 프로그램 설계',
      '비즈니스 전략'
    ],
    tone: 'professional',
    writingStyle: 'detailed',
    targetAudience: '헬스장 운영에 관심있는 사람, 창업 준비생',
    experienceYears: 10,
    credentials: [
      '생활체육지도자 2급',
      '헬스장 운영 10년',
      '누적 회원 1,000명 이상 지도'
    ],
    sampleIntro: '10년간 헬스장을 운영하면서 수많은 회원들의 변화를 지켜봤습니다. 제 경험을 바탕으로...',
    avatar: '/avatars/gym-owner.png'
  },
  
  // 2. 20대 여자 트레이너
  {
    id: 'female-trainer-20s',
    name: '20대 여성 퍼스널 트레이너',
    category: 'trainer',
    description: '젊은 감각의 여성 트레이너. SNS를 잘 활용하며 20-30대 여성 회원에게 인기',
    expertise: [
      '여성 체형 관리',
      '다이어트 프로그램',
      '홈트레이닝',
      '식단 관리'
    ],
    tone: 'friendly',
    writingStyle: 'storytelling',
    targetAudience: '20-30대 여성, 다이어트에 관심있는 사람',
    experienceYears: 3,
    credentials: [
      '생활체육지도자 2급',
      'NSCA-CPT 자격',
      '인스타그램 팔로워 2만명'
    ],
    sampleIntro: '안녕하세요! 20대 여성들의 다이어트 고민을 가장 잘 이해하는 트레이너 OOO입니다 😊',
    avatar: '/avatars/female-trainer.png'
  },
  
  // 3. 의학 석사
  {
    id: 'medical-master',
    name: '의학 석사 (스포츠의학)',
    category: 'medical',
    description: '스포츠의학 전공 석사. 과학적 근거 기반의 운동법과 재활 전문',
    expertise: [
      '운동 생리학',
      '스포츠 손상 예방',
      '재활 운동',
      '과학적 운동법'
    ],
    tone: 'educational',
    writingStyle: 'datadriven',
    targetAudience: '근거 기반 정보를 원하는 사람, 전문가',
    experienceYears: 8,
    credentials: [
      '스포츠의학 석사',
      '물리치료사',
      'SCI급 논문 5편 게재'
    ],
    sampleIntro: '스포츠의학 석사로서, 과학적 근거를 바탕으로 안전하고 효과적인 운동법을 알려드리겠습니다.',
    avatar: '/avatars/medical-expert.png'
  },
  
  // 4. 필라테스 강사
  {
    id: 'pilates-instructor',
    name: '필라테스 전문 강사',
    category: 'instructor',
    description: '5년 경력의 필라테스 강사. 자세 교정과 코어 강화 전문',
    expertise: [
      '필라테스',
      '자세 교정',
      '코어 강화',
      '유연성 향상'
    ],
    tone: 'friendly',
    writingStyle: 'concise',
    targetAudience: '필라테스 초보자, 자세 교정이 필요한 사람',
    experienceYears: 5,
    credentials: [
      'PMA 인증 필라테스 강사',
      'Mat & Reformer 자격',
      '누적 수강생 500명'
    ],
    sampleIntro: '몸의 중심인 코어를 강화하고 바른 자세를 찾아가는 여정, 함께 시작해볼까요?',
    avatar: '/avatars/pilates-instructor.png'
  },
  
  // 5. FC (프랜차이즈 센터장)
  {
    id: 'franchise-center',
    name: '대형 피트니스 FC 센터장',
    category: 'franchise',
    description: '300평 규모 프랜차이즈 피트니스 센터 운영. 체계적인 시스템과 마케팅 노하우',
    expertise: [
      '센터 운영 시스템',
      '마케팅 전략',
      '직원 관리',
      'GX 프로그램 운영'
    ],
    tone: 'motivational',
    writingStyle: 'detailed',
    targetAudience: '피트니스 창업 준비생, 센터 운영자',
    experienceYears: 7,
    credentials: [
      '애니타임 피트니스 FC',
      '7년 운영 경험',
      '월 평균 회원 500명'
    ],
    sampleIntro: '프랜차이즈 센터 운영 7년차, 성공적인 센터 운영 노하우를 공유합니다.',
    avatar: '/avatars/franchise-owner.png'
  },
  
  // 6. 크로스핏 코치
  {
    id: 'crossfit-coach',
    name: '크로스핏 레벨3 코치',
    category: 'trainer',
    description: '고강도 기능성 운동 전문. 크로스핏 대회 입상 경력',
    expertise: [
      '크로스핏',
      '기능성 운동',
      '고강도 트레이닝',
      '올림픽 리프팅'
    ],
    tone: 'motivational',
    writingStyle: 'storytelling',
    targetAudience: '크로스핏에 관심있는 사람, 고강도 운동 선호자',
    experienceYears: 6,
    credentials: [
      'CrossFit Level 3 Trainer',
      '전국 대회 입상',
      'USAW 자격증'
    ],
    sampleIntro: '한계를 넘어서는 순간, 진짜 성장이 시작됩니다. 크로스핏으로 함께 도전해요!',
    avatar: '/avatars/crossfit-coach.png'
  },
  
  // 7. 보디빌더
  {
    id: 'bodybuilder',
    name: '현역 보디빌더',
    category: 'trainer',
    description: '대회 입상 경력의 보디빌더. 근육 성장과 식단 전문',
    expertise: [
      '근비대',
      '보디빌딩',
      '대회 준비',
      '벌크업/린매스'
    ],
    tone: 'professional',
    writingStyle: 'detailed',
    targetAudience: '근육 성장을 원하는 남성, 보디빌딩 관심자',
    experienceYears: 8,
    credentials: [
      '미스터코리아 입상',
      '보디빌딩 8년',
      '대회 준비 전문'
    ],
    sampleIntro: '근육 하나하나를 정확히 공략하는 방법, 대회 준비 노하우를 알려드립니다.',
    avatar: '/avatars/bodybuilder.png'
  },
  
  // 8. 재활 전문 트레이너
  {
    id: 'rehab-trainer',
    name: '재활 운동 전문 트레이너',
    category: 'medical',
    description: '부상 후 재활과 통증 관리 전문. 중장년층 지도 경험 풍부',
    expertise: [
      '재활 운동',
      '통증 관리',
      '관절 강화',
      '중장년 운동'
    ],
    tone: 'professional',
    writingStyle: 'educational',
    targetAudience: '부상 경험자, 통증이 있는 사람, 중장년층',
    experienceYears: 10,
    credentials: [
      '재활 트레이너 자격',
      '물리치료 보조',
      '10년 경력'
    ],
    sampleIntro: '통증 없이 안전하게 운동하는 방법, 재활의 모든 것을 알려드립니다.',
    avatar: '/avatars/rehab-trainer.png'
  },
]
```

---

### 5.2 AI 글쓰기 화면 (/ai-writer/new)

#### UI 레이아웃

```
┌─────────────────────────────────────────────┐
│ AI 블로그 글쓰기                            │
└─────────────────────────────────────────────┘

┌─ Step 1: 페르소나 선택 ─────────────────────┐
│                                              │
│ 어떤 전문가로 글을 쓰시겠어요?              │
│                                              │
│ [그리드 레이아웃 - 8개 카드]                │
│                                              │
│ ┌───────────┐ ┌───────────┐ ┌───────────┐ │
│ │ 👔        │ │ 💪        │ │ 🔬        │ │
│ │ 10년차    │ │ 20대 여자 │ │ 의학 석사 │ │
│ │ 관장      │ │ 트레이너  │ │           │ │
│ │           │ │           │ │           │ │
│ │ [선택]    │ │ [선택]    │ │ [선택]    │ │
│ └───────────┘ └───────────┘ └───────────┘ │
│                                              │
│ ┌───────────┐ ┌───────────┐ ┌───────────┐ │
│ │ 🧘        │ │ 🏢        │ │ 🔥        │ │
│ │ 필라테스  │ │ FC 센터장 │ │ 크로스핏  │ │
│ │ 강사      │ │           │ │ 코치      │ │
│ │           │ │           │ │           │ │
│ │ [선택]    │ │ [선택]    │ │ [선택]    │ │
│ └───────────┘ └───────────┘ └───────────┘ │
│                                              │
│ ... 2개 더                                   │
│                                              │
└──────────────────────────────────────────────┘

┌─ Step 2: 글 주제 & 설정 ─────────────────────┐
│                                              │
│ 선택된 페르소나: 10년차 관장 👔              │
│                                              │
│ 카테고리 선택:                               │
│ ○ 운동 가이드                                │
│ ● 운동 루틴                                  │
│ ○ 영양/식단                                  │
│ ○ 다이어트                                   │
│ ○ 근육 성장                                  │
│ ○ 재활/부상 예방                             │
│                                              │
│ 키워드 입력:                                 │
│ [헬스장 초보자를 위한 3개월 루틴           ]│
│                                              │
│ 대상 독자:                                   │
│ ○ 초보자    ● 중급자    ○ 고급자            │
│                                              │
│ 글 분량:                                     │
│ ○ 짧게 (1,000자)                            │
│ ● 보통 (2,000자)                            │
│ ○ 길게 (3,000자)                            │
│                                              │
│ 추가 요구사항 (선택):                        │
│ [개인 경험담을 많이 넣어주세요             ]│
│                                              │
│ [취소]                    [AI 생성하기 →]   │
│                                              │
└──────────────────────────────────────────────┘

┌─ Step 3: AI 생성 중 ─────────────────────────┐
│                                              │
│              🤖 AI가 글을 작성하고 있어요    │
│                                              │
│              ━━━━━━━━━━━━━━━━ 75%          │
│                                              │
│         ✓ 아이디어 구상 완료                │
│         ✓ 목차 구성 완료                    │
│         ⏳ 본문 작성 중... (30초 남음)      │
│         ⏱ 마무리 및 검토 대기               │
│                                              │
│                                              │
│         💡 잠깐! 알고 계셨나요?              │
│                                              │
│         "헬스장 초보자" 키워드는            │
│         월 검색량 12,000회예요               │
│         경쟁도는 '중'으로 공략 가능합니다!  │
│                                              │
└──────────────────────────────────────────────┘

┌─ Step 4: 결과 미리보기 & 편집 ───────────────┐
│                                              │
│ ✅ AI 글쓰기 완료!                           │
│                                              │
│ ┌─ 좌측: 메타 정보 ───────────────────────┐ │
│ │                                          │ │
│ │ 📝 제목 (5개 추천)                       │ │
│ │ ● 헬스장 초보자를 위한 완벽 3개월 루틴  │ │
│ │ ○ 처음 헬스장 가는 분들을 위한 가이드   │ │
│ │ ○ 초보 탈출! 3개월 헬스 프로그램        │ │
│ │ ○ 10년차 관장이 알려주는 초보 루틴     │ │
│ │ ○ 초보자도 따라하는 체계적 헬스 계획   │ │
│ │                                          │ │
│ │ 🏷️ 태그 (10개)                          │ │
│ │ #헬스장초보 #운동루틴 #3개월            │ │
│ │ #근력운동 #헬스프로그램 ...              │ │
│ │                                          │ │
│ │ 📊 SEO 점수: 92/100 ⭐                   │ │
│ │ ✓ 키워드 밀도: 최적                     │ │
│ │ ✓ 가독성: 우수                          │ │
│ │ ✓ 원본성: 100% 유니크                   │ │
│ │                                          │ │
│ │ 📸 추천 이미지 (3개)                     │ │
│ │ [썸네일1] [썸네일2] [썸네일3]           │ │
│ │                                          │ │
│ └──────────────────────────────────────────┘ │
│                                              │
│ ┌─ 우측: 본문 편집기 ──────────────────────┐ │
│ │                                          │ │
│ │ [Tiptap 리치 에디터]                     │ │
│ │                                          │ │
│ │ # 헬스장 초보자를 위한 완벽 3개월 루틴  │ │
│ │                                          │ │
│ │ 안녕하세요, 10년간 헬스장을 운영하며... │ │
│ │                                          │ │
│ │ ## 1개월차: 기초 다지기                 │ │
│ │ 첫 달은 가장 중요합니다...               │ │
│ │                                          │ │
│ │ [2000자 본문 전체]                       │ │
│ │                                          │ │
│ │ [실시간 자동 저장됨 ✓]                  │ │
│ │                                          │ │
│ └──────────────────────────────────────────┘ │
│                                              │
│ ┌─ 하단 액션 ─────────────────────────────┐ │
│ │                                          │ │
│ │ [🔄 다시 생성]  [💾 저장]  [🚀 발행하기]│ │
│ │                                          │ │
│ └──────────────────────────────────────────┘ │
│                                              │
└──────────────────────────────────────────────┘
```

#### 컴포넌트 구현

```typescript
// app/(dashboard)/ai-writer/new/page.tsx
'use client'

import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { PersonaSelector } from '@/components/ai-writer/PersonaSelector'
import { ContentSettings } from '@/components/ai-writer/ContentSettings'
import { GenerationProgress } from '@/components/ai-writer/GenerationProgress'
import { ContentEditor } from '@/components/ai-writer/ContentEditor'

export default function NewAIWriterPage() {
  const router = useRouter()
  const [step, setStep] = useState<1 | 2 | 3 | 4>(1)
  const [selectedPersona, setSelectedPersona] = useState<Persona | null>(null)
  const [settings, setSettings] = useState<ContentSettings | null>(null)
  const [generatedContent, setGeneratedContent] = useState<GeneratedContent | null>(null)
  
  const handlePersonaSelect = (persona: Persona) => {
    setSelectedPersona(persona)
    setStep(2)
  }
  
  const handleGenerate = async (settings: ContentSettings) => {
    setSettings(settings)
    setStep(3)
    
    try {
      // AI 생성 API 호출
      const result = await generateContent({
        persona: selectedPersona,
        ...settings
      })
      
      setGeneratedContent(result)
      setStep(4)
    } catch (error) {
      console.error('생성 실패:', error)
      // 에러 처리
    }
  }
  
  const handlePublish = async () => {
    // 발행 처리
    router.push('/publish')
  }
  
  return (
    <div className="max-w-7xl mx-auto">
      {/* Progress Steps */}
      <div className="mb-8">
        <Steps current={step} />
      </div>
      
      {/* Step Content */}
      {step === 1 && (
        <PersonaSelector onSelect={handlePersonaSelect} />
      )}
      
      {step === 2 && selectedPersona && (
        <ContentSettings
          persona={selectedPersona}
          onGenerate={handleGenerate}
          onBack={() => setStep(1)}
        />
      )}
      
      {step === 3 && (
        <GenerationProgress settings={settings} />
      )}
      
      {step === 4 && generatedContent && (
        <ContentEditor
          content={generatedContent}
          onPublish={handlePublish}
          onRegenerate={() => setStep(2)}
        />
      )}
    </div>
  )
}

// components/ai-writer/PersonaSelector.tsx
export function PersonaSelector({ 
  onSelect 
}: {
  onSelect: (persona: Persona) => void
}) {
  return (
    <div className="space-y-6">
      <div>
        <h2 className="text-2xl font-bold mb-2">
          어떤 전문가로 글을 쓰시겠어요?
        </h2>
        <p className="text-gray-600">
          선택한 페르소나의 스타일로 AI가 글을 작성합니다
        </p>
      </div>
      
      <div className="grid md:grid-cols-4 gap-4">
        {PERSONAS.map((persona) => (
          <PersonaCard
            key={persona.id}
            persona={persona}
            onSelect={() => onSelect(persona)}
          />
        ))}
      </div>
    </div>
  )
}

function PersonaCard({ 
  persona, 
  onSelect 
}: {
  persona: Persona
  onSelect: () => void
}) {
  return (
    <Card 
      className="cursor-pointer hover:shadow-lg transition-all hover:border-primary-500"
      onClick={onSelect}
    >
      <CardContent className="p-6 text-center space-y-4">
        {/* 아바타 */}
        <div className="w-20 h-20 mx-auto rounded-full bg-gradient-to-br from-primary-100 to-info-100 flex items-center justify-center text-4xl">
          {persona.avatar ? (
            <Image src={persona.avatar} alt={persona.name} width={80} height={80} />
          ) : (
            <span>👔</span>
          )}
        </div>
        
        {/* 이름 */}
        <div>
          <h3 className="font-semibold text-lg mb-1">
            {persona.name}
          </h3>
          <p className="text-sm text-gray-500">
            {persona.experienceYears}년 경력
          </p>
        </div>
        
        {/* 전문 분야 */}
        <div className="flex flex-wrap gap-1 justify-center">
          {persona.expertise.slice(0, 2).map((skill) => (
            <Badge key={skill} variant="secondary" className="text-xs">
              {skill}
            </Badge>
          ))}
        </div>
        
        {/* 설명 */}
        <p className="text-sm text-gray-600 line-clamp-2">
          {persona.description}
        </p>
        
        {/* 선택 버튼 */}
        <Button className="w-full" variant="outline">
          선택하기
        </Button>
      </CardContent>
    </Card>
  )
}

// components/ai-writer/ContentSettings.tsx
export function ContentSettings({
  persona,
  onGenerate,
  onBack
}: {
  persona: Persona
  onGenerate: (settings: ContentSettings) => void
  onBack: () => void
}) {
  const [category, setCategory] = useState<ContentCategory>('exercise-guide')
  const [keyword, setKeyword] = useState('')
  const [audience, setAudience] = useState<'beginner' | 'intermediate' | 'advanced'>('beginner')
  const [length, setLength] = useState<1000 | 2000 | 3000>(2000)
  const [additionalNotes, setAdditionalNotes] = useState('')
  
  const handleSubmit = () => {
    onGenerate({
      personaId: persona.id,
      category,
      keyword,
      audience,
      length,
      additionalNotes
    })
  }
  
  return (
    <div className="max-w-2xl mx-auto space-y-6">
      {/* 선택된 페르소나 표시 */}
      <Card className="bg-gradient-to-br from-primary-50 to-info-50">
        <CardContent className="p-4 flex items-center gap-4">
          <div className="w-12 h-12 rounded-full bg-white flex items-center justify-center">
            👔
          </div>
          <div>
            <p className="text-sm text-gray-600">선택된 페르소나</p>
            <p className="font-semibold">{persona.name}</p>
          </div>
          <Button variant="ghost" size="sm" onClick={onBack} className="ml-auto">
            변경
          </Button>
        </CardContent>
      </Card>
      
      {/* 카테고리 선택 */}
      <div>
        <Label className="text-base font-semibold mb-3 block">
          카테고리 선택
        </Label>
        <div className="grid grid-cols-2 gap-3">
          <CategoryButton
            label="운동 가이드"
            icon="💪"
            selected={category === 'exercise-guide'}
            onClick={() => setCategory('exercise-guide')}
          />
          <CategoryButton
            label="운동 루틴"
            icon="📋"
            selected={category === 'routine'}
            onClick={() => setCategory('routine')}
          />
          <CategoryButton
            label="영양/식단"
            icon="🍎"
            selected={category === 'nutrition'}
            onClick={() => setCategory('nutrition')}
          />
          <CategoryButton
            label="다이어트"
            icon="⚖️"
            selected={category === 'diet'}
            onClick={() => setCategory('diet')}
          />
          <CategoryButton
            label="근육 성장"
            icon="💪"
            selected={category === 'muscle'}
            onClick={() => setCategory('muscle')}
          />
          <CategoryButton
            label="재활/부상"
            icon="🏥"
            selected={category === 'rehab'}
            onClick={() => setCategory('rehab')}
          />
        </div>
      </div>
      
      {/* 키워드 입력 */}
      <div>
        <Label htmlFor="keyword" className="text-base font-semibold mb-3 block">
          키워드 입력
        </Label>
        <Input
          id="keyword"
          placeholder="예: 헬스장 초보자를 위한 3개월 루틴"
          value={keyword}
          onChange={(e) => setKeyword(e.target.value)}
          className="text-base"
        />
        <p className="text-sm text-gray-500 mt-2">
          💡 구체적으로 입력할수록 좋은 결과를 얻을 수 있어요
        </p>
      </div>
      
      {/* 대상 독자 */}
      <div>
        <Label className="text-base font-semibold mb-3 block">
          대상 독자
        </Label>
        <div className="flex gap-3">
          <RadioButton
            label="초보자"
            checked={audience === 'beginner'}
            onChange={() => setAudience('beginner')}
          />
          <RadioButton
            label="중급자"
            checked={audience === 'intermediate'}
            onChange={() => setAudience('intermediate')}
          />
          <RadioButton
            label="고급자"
            checked={audience === 'advanced'}
            onChange={() => setAudience('advanced')}
          />
        </div>
      </div>
      
      {/* 글 분량 */}
      <div>
        <Label className="text-base font-semibold mb-3 block">
          글 분량
        </Label>
        <div className="flex gap-3">
          <RadioButton
            label="짧게 (1,000자)"
            checked={length === 1000}
            onChange={() => setLength(1000)}
          />
          <RadioButton
            label="보통 (2,000자)"
            checked={length === 2000}
            onChange={() => setLength(2000)}
          />
          <RadioButton
            label="길게 (3,000자)"
            checked={length === 3000}
            onChange={() => setLength(3000)}
          />
        </div>
      </div>
      
      {/* 추가 요구사항 */}
      <div>
        <Label htmlFor="notes" className="text-base font-semibold mb-3 block">
          추가 요구사항 (선택)
        </Label>
        <Textarea
          id="notes"
          placeholder="예: 개인 경험담을 많이 넣어주세요"
          value={additionalNotes}
          onChange={(e) => setAdditionalNotes(e.target.value)}
          rows={3}
        />
      </div>
      
      {/* 액션 버튼 */}
      <div className="flex gap-3 pt-4">
        <Button variant="outline" onClick={onBack} className="flex-1">
          취소
        </Button>
        <Button 
          onClick={handleSubmit}
          disabled={!keyword}
          className="flex-1 gap-2"
        >
          <Sparkles className="w-4 h-4" />
          AI 생성하기
        </Button>
      </div>
    </div>
  )
}

function CategoryButton({ 
  label, 
  icon, 
  selected, 
  onClick 
}: {
  label: string
  icon: string
  selected: boolean
  onClick: () => void
}) {
  return (
    <button
      onClick={onClick}
      className={`
        p-4 rounded-lg border-2 transition-all
        ${selected 
          ? 'border-primary-500 bg-primary-50' 
          : 'border-gray-200 hover:border-gray-300'
        }
      `}
    >
      <div className="text-2xl mb-2">{icon}</div>
      <div className="font-medium">{label}</div>
    </button>
  )
}

// components/ai-writer/GenerationProgress.tsx
export function GenerationProgress({ settings }) {
  const [progress, setProgress] = useState(0)
  const [currentStep, setCurrentStep] = useState('아이디어 구상')
  
  useEffect(() => {
    // 진행 상태 시뮬레이션
    const steps = [
      { label: '아이디어 구상', duration: 2000 },
      { label: '목차 구성', duration: 3000 },
      { label: '본문 작성', duration: 10000 },
      { label: '마무리 및 검토', duration: 3000 }
    ]
    
    let currentProgress = 0
    let stepIndex = 0
    
    const interval = setInterval(() => {
      if (stepIndex < steps.length) {
        setCurrentStep(steps[stepIndex].label)
        currentProgress += 25
        setProgress(currentProgress)
        stepIndex++
      }
    }, steps[stepIndex]?.duration || 1000)
    
    return () => clearInterval(interval)
  }, [])
  
  return (
    <div className="max-w-2xl mx-auto">
      <Card>
        <CardContent className="p-12 text-center space-y-8">
          {/* 아이콘 */}
          <motion.div
            animate={{ rotate: 360 }}
            transition={{ duration: 2, repeat: Infinity, ease: 'linear' }}
            className="w-20 h-20 mx-auto"
          >
            <Sparkles className="w-20 h-20 text-primary-500" />
          </motion.div>
          
          {/* 메시지 */}
          <div>
            <h2 className="text-2xl font-bold mb-2">
              AI가 글을 작성하고 있어요
            </h2>
            <p className="text-gray-600">
              {settings.keyword}
            </p>
          </div>
          
          {/* 프로그레스 바 */}
          <div className="space-y-2">
            <div className="flex justify-between text-sm text-gray-600">
              <span>{currentStep}</span>
              <span>{progress}%</span>
            </div>
            <div className="w-full h-3 bg-gray-200 rounded-full overflow-hidden">
              <motion.div
                className="h-full bg-gradient-primary"
                initial={{ width: 0 }}
                animate={{ width: `${progress}%` }}
                transition={{ duration: 0.5 }}
              />
            </div>
          </div>
          
          {/* 단계 체크리스트 */}
          <div className="space-y-2 text-left max-w-xs mx-auto">
            <StepItem label="아이디어 구상" completed={progress > 0} />
            <StepItem label="목차 구성" completed={progress > 25} />
            <StepItem label="본문 작성" completed={progress > 50} current={progress > 25 && progress <= 75} />
            <StepItem label="마무리 및 검토" completed={progress > 75} current={progress > 75} />
          </div>
          
          {/* 팁 */}
          <Card className="bg-info-50 border-info-200">
            <CardContent className="p-4 text-left">
              <p className="text-sm font-semibold mb-1">💡 잠깐! 알고 계셨나요?</p>
              <p className="text-sm text-gray-600">
                "{settings.keyword}" 키워드는 월 검색량 12,000회예요.
                경쟁도는 '중'으로 공략 가능합니다!
              </p>
            </CardContent>
          </Card>
        </CardContent>
      </Card>
    </div>
  )
}

function StepItem({ 
  label, 
  completed, 
  current 
}: {
  label: string
  completed: boolean
  current?: boolean
}) {
  return (
    <div className="flex items-center gap-3">
      {completed ? (
        <Check className="w-5 h-5 text-success-500" />
      ) : current ? (
        <Loader2 className="w-5 h-5 text-primary-500 animate-spin" />
      ) : (
        <div className="w-5 h-5 rounded-full border-2 border-gray-300" />
      )}
      <span className={completed || current ? 'font-medium' : 'text-gray-500'}>
        {label}
      </span>
    </div>
  )
}
```

---

## 6. 핵심 기능: 이미지 생성 시스템

### 6.1 나노바나나 API 통합

```typescript
// lib/nanobanana.ts

const NANOBANANA_API_URL = 'https://api.nolowai.com/v1'

export interface ImageGenerationRequest {
  prompt: string
  negativePrompt?: string
  width?: number
  height?: number
  style?: 'realistic' | 'anime' | 'illustration'
}

export interface ImageGenerationResponse {
  id: string
  imageUrl: string
  prompt: string
  width: number
  height: number
  createdAt: string
}

export async function generateImage(
  request: ImageGenerationRequest
): Promise<ImageGenerationResponse> {
  const response = await fetch(`${NANOBANANA_API_URL}/generate`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      // 나노바나나는 무료이므로 API 키 불필요
    },
    body: JSON.stringify({
      prompt: request.prompt,
      negative_prompt: request.negativePrompt || '저품질, 흐릿한, 왜곡된',
      width: request.width || 512,
      height: request.height || 512,
      style: request.style || 'realistic',
    }),
  })
  
  if (!response.ok) {
    throw new Error('이미지 생성 실패')
  }
  
  const data = await response.json()
  
  return {
    id: data.id,
    imageUrl: data.image_url,
    prompt: data.prompt,
    width: data.width,
    height: data.height,
    createdAt: data.created_at,
  }
}

// 피트니스 특화 프롬프트 템플릿
export const FITNESS_IMAGE_PROMPTS = {
  'exercise-squat': {
    ko: '스쿼트 운동 자세, 측면 각도, 올바른 form, 헬스장 배경, 고품질 사진',
    en: 'person doing squat exercise, side view, proper form, gym background, high quality photo'
  },
  'exercise-bench-press': {
    ko: '벤치프레스 운동, 측면 각도, 올바른 자세, 헬스장 배경',
    en: 'person doing bench press, side view, proper form, gym equipment, professional photo'
  },
  'home-workout': {
    ko: '홈트레이닝, 거실에서 운동하는 사람, 요가 매트, 자연광',
    en: 'home workout, person exercising in living room, yoga mat, natural lighting'
  },
  'nutrition': {
    ko: '건강한 식단, 단백질 식품, 야채, 과일, 플레이팅, 위에서 본 각도',
    en: 'healthy meal, protein foods, vegetables, fruits, well plated, top view'
  },
  'gym-equipment': {
    ko: '헬스장 기구, 덤벨, 바벨, 깨끗한 배경, 프로페셔널 사진',
    en: 'gym equipment, dumbbells, barbells, clean background, professional photo'
  },
  'before-after': {
    ko: '체형 변화 비교, 전후 사진, 깨끗한 배경',
    en: 'body transformation comparison, before and after, clean background'
  },
}

// 프롬프트 자동 생성
export function generateFitnessPrompt(
  category: string,
  keyword: string
): string {
  const template = FITNESS_IMAGE_PROMPTS[category]
  
  if (!template) {
    // 키워드 기반 자동 생성
    return `${keyword}, 피트니스 관련, 고품질 사진, 전문적인`
  }
  
  return template.en
}
```

### 6.2 이미지 생성 화면 (/images/generate)

```typescript
// app/(dashboard)/images/generate/page.tsx
'use client'

export default function ImageGeneratePage() {
  const [prompt, setPrompt] = useState('')
  const [category, setCategory] = useState<string>('')
  const [isGenerating, setIsGenerating] = useState(false)
  const [generatedImage, setGeneratedImage] = useState<string | null>(null)
  
  const handleGenerate = async () => {
    setIsGenerating(true)
    
    try {
      const fullPrompt = category 
        ? generateFitnessPrompt(category, prompt)
        : prompt
      
      const result = await generateImage({
        prompt: fullPrompt,
        width: 512,
        height: 512,
        style: 'realistic'
      })
      
      setGeneratedImage(result.imageUrl)
      
      // 생성된 이미지 저장
      await saveGeneratedImage(result)
      
    } catch (error) {
      console.error('생성 실패:', error)
    } finally {
      setIsGenerating(false)
    }
  }
  
  return (
    <div className="max-w-6xl mx-auto space-y-6">
      <div>
        <h1 className="text-3xl font-bold mb-2">AI 이미지 생성</h1>
        <p className="text-gray-600">
          나노바나나 AI로 무료로 이미지를 생성하세요
        </p>
      </div>
      
      <div className="grid lg:grid-cols-2 gap-6">
        {/* 왼쪽: 설정 */}
        <Card>
          <CardHeader>
            <CardTitle>이미지 설정</CardTitle>
          </CardHeader>
          <CardContent className="space-y-4">
            {/* 카테고리 */}
            <div>
              <Label>카테고리 (선택)</Label>
              <Select value={category} onValueChange={setCategory}>
                <SelectTrigger>
                  <SelectValue placeholder="템플릿 선택..." />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="exercise-squat">스쿼트 자세</SelectItem>
                  <SelectItem value="exercise-bench-press">벤치프레스</SelectItem>
                  <SelectItem value="home-workout">홈트레이닝</SelectItem>
                  <SelectItem value="nutrition">건강 식단</SelectItem>
                  <SelectItem value="gym-equipment">헬스 기구</SelectItem>
                </SelectContent>
              </Select>
              {category && (
                <p className="text-sm text-gray-500 mt-2">
                  💡 "{category}" 템플릿이 적용됩니다
                </p>
              )}
            </div>
            
            {/* 프롬프트 */}
            <div>
              <Label>프롬프트</Label>
              <Textarea
                placeholder="예: 덤벨로 운동하는 여성 트레이너"
                value={prompt}
                onChange={(e) => setPrompt(e.target.value)}
                rows={4}
              />
            </div>
            
            {/* 생성 버튼 */}
            <Button 
              onClick={handleGenerate}
              disabled={!prompt || isGenerating}
              className="w-full gap-2"
              size="lg"
            >
              {isGenerating ? (
                <>
                  <Loader2 className="w-4 h-4 animate-spin" />
                  생성 중...
                </>
              ) : (
                <>
                  <Sparkles className="w-4 h-4" />
                  이미지 생성하기
                </>
              )}
            </Button>
            
            <p className="text-xs text-gray-500 text-center">
              ✨ 나노바나나 AI 무료 제공
            </p>
          </CardContent>
        </Card>
        
        {/* 오른쪽: 결과 */}
        <Card>
          <CardHeader>
            <CardTitle>생성 결과</CardTitle>
          </CardHeader>
          <CardContent>
            {isGenerating ? (
              <div className="aspect-square bg-gray-100 rounded-lg flex items-center justify-center">
                <div className="text-center">
                  <Loader2 className="w-12 h-12 text-primary-500 animate-spin mx-auto mb-4" />
                  <p className="text-gray-600">AI가 이미지를 생성하고 있어요...</p>
                </div>
              </div>
            ) : generatedImage ? (
              <div className="space-y-4">
                <div className="aspect-square bg-gray-100 rounded-lg overflow-hidden">
                  <Image
                    src={generatedImage}
                    alt="생성된 이미지"
                    width={512}
                    height={512}
                    className="w-full h-full object-cover"
                  />
                </div>
                
                <div className="flex gap-2">
                  <Button variant="outline" className="flex-1">
                    다운로드
                  </Button>
                  <Button variant="outline" className="flex-1">
                    라이브러리에 저장
                  </Button>
                  <Button className="flex-1">
                    글에 추가
                  </Button>
                </div>
              </div>
            ) : (
              <div className="aspect-square bg-gray-50 rounded-lg flex items-center justify-center">
                <div className="text-center text-gray-400">
                  <ImageIcon className="w-12 h-12 mx-auto mb-2" />
                  <p>생성된 이미지가 여기 표시됩니다</p>
                </div>
              </div>
            )}
          </CardContent>
        </Card>
      </div>
      
      {/* 이미지 라이브러리 */}
      <Card>
        <CardHeader>
          <CardTitle>최근 생성한 이미지</CardTitle>
        </CardHeader>
        <CardContent>
          <ImageLibrary userId={user.id} limit={6} />
        </CardContent>
      </Card>
    </div>
  )
}
```

---

## 7. API 명세

### 7.1 인증 API

```typescript
// POST /api/auth/signup
interface SignupRequest {
  email: string
  password: string
  nickname: string
  blogUrl?: string
}

interface SignupResponse {
  user: {
    id: string
    email: string
    nickname: string
  }
  accessToken: string
  refreshToken: string
}

// POST /api/auth/login
interface LoginRequest {
  email: string
  password: string
}

interface LoginResponse {
  user: {
    id: string
    email: string
    nickname: string
  }
  accessToken: string
  refreshToken: string
}
```

### 7.2 AI 생성 API

```typescript
// POST /api/ai/generate-content
interface GenerateContentRequest {
  personaId: string
  category: ContentCategory
  keyword: string
  audience: 'beginner' | 'intermediate' | 'advanced'
  length: 1000 | 2000 | 3000
  additionalNotes?: string
}

interface GenerateContentResponse {
  id: string
  titles: string[]  // 5개 추천 제목
  body: string      // 본문
  metaDescription: string
  tags: string[]    // 10개 태그
  images: RecommendedImage[]
  seoScore: number
  uniquenessScore: number
  qualityScore: number
}

// POST /api/ai/generate-image
interface GenerateImageRequest {
  prompt: string
  category?: string
  width?: number
  height?: number
}

interface GenerateImageResponse {
  id: string
  imageUrl: string
  prompt: string
}
```

### 7.3 키워드 API

```typescript
// GET /api/keywords/search?keyword=홈트레이닝
interface KeywordSearchResponse {
  keyword: string
  monthlySearchVolume: number
  competition: 'low' | 'medium' | 'high'
  pcRatio: number
  mobileRatio: number
  trend: number[]  // 12개월 데이터
  relatedKeywords: RelatedKeyword[]
}

// GET /api/keywords/recommendations?category=fitness
interface KeywordRecommendationsResponse {
  keywords: {
    keyword: string
    searchVolume: number
    competition: string
    opportunityScore: number  // 0-100
  }[]
}
```

### 7.4 커뮤니티 API

```typescript
// GET /api/community/missions/today
interface TodayMissionsResponse {
  missions: {
    id: string
    type: 'read' | 'comment' | 'like' | 'publish'
    requirement: number
    completed: number
    points: number
  }[]
  totalPoints: number
  completionRate: number
}

// POST /api/community/missions/complete
interface CompleteMissionRequest {
  missionId: string
  proof?: string  // 증거 (URL, 스크린샷 등)
}

interface CompleteMissionResponse {
  success: boolean
  pointsEarned: number
  newBalance: number
}

// GET /api/community/leaderboard?groupId=xxx
interface LeaderboardResponse {
  leaderboard: {
    rank: number
    userId: string
    nickname: string
    points: number
    change: number
  }[]
  myRank: number
}
```

---

## 8. 데이터베이스 스키마

```sql
-- 사용자
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email VARCHAR(255) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  nickname VARCHAR(50) UNIQUE NOT NULL,
  
  -- 블로그 정보
  blog_url VARCHAR(500),
  blog_platform VARCHAR(20), -- naver, tistory
  blog_index INTEGER DEFAULT 0,
  blog_level VARCHAR(20) DEFAULT 'beginner',
  
  -- 구독
  subscription_tier VARCHAR(20) DEFAULT 'free',
  subscription_expires_at TIMESTAMP,
  
  -- 포인트 & 레벨
  points INTEGER DEFAULT 0,
  level INTEGER DEFAULT 1,
  xp INTEGER DEFAULT 0,
  
  -- 활동
  streak_days INTEGER DEFAULT 0,
  last_activity_date DATE,
  
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- AI 생성 콘텐츠
CREATE TABLE generated_contents (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  
  -- 페르소나 정보
  persona_id VARCHAR(50) NOT NULL,
  persona_name VARCHAR(100),
  
  -- 설정
  category VARCHAR(50) NOT NULL,
  keyword VARCHAR(200) NOT NULL,
  audience VARCHAR(20),
  target_length INTEGER,
  
  -- 생성 결과
  titles TEXT[] NOT NULL,  -- 배열
  selected_title TEXT,
  body TEXT NOT NULL,
  meta_description TEXT,
  tags TEXT[],
  
  -- 이미지
  recommended_images JSONB,
  selected_images JSONB,
  
  -- 품질 지표
  seo_score FLOAT,
  uniqueness_score FLOAT,
  quality_score FLOAT,
  
  -- 상태
  status VARCHAR(20) DEFAULT 'draft', -- draft, published, archived
  published_at TIMESTAMP,
  naver_post_url VARCHAR(500),
  
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_generated_contents_user ON generated_contents(user_id);
CREATE INDEX idx_generated_contents_status ON generated_contents(status);

-- AI 생성 이미지
CREATE TABLE generated_images (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  
  prompt TEXT NOT NULL,
  category VARCHAR(50),
  
  -- 이미지 정보
  image_url TEXT NOT NULL,
  width INTEGER,
  height INTEGER,
  
  -- 사용 정보
  used_in_content_id UUID REFERENCES generated_contents(id),
  
  created_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_generated_images_user ON generated_images(user_id);

-- 커뮤니티 챌린지 그룹
CREATE TABLE challenge_groups (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(100) NOT NULL,
  level VARCHAR(20) NOT NULL, -- beginner, growing, mature
  
  start_date DATE NOT NULL,
  end_date DATE NOT NULL,
  
  target_goal TEXT,
  status VARCHAR(20) DEFAULT 'active', -- active, completed
  
  created_at TIMESTAMP DEFAULT NOW()
);

-- 그룹 멤버
CREATE TABLE group_members (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  group_id UUID REFERENCES challenge_groups(id) ON DELETE CASCADE,
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  
  role VARCHAR(20) DEFAULT 'member', -- leader, member
  
  -- 활동 통계
  total_points INTEGER DEFAULT 0,
  missions_completed INTEGER DEFAULT 0,
  
  joined_at TIMESTAMP DEFAULT NOW(),
  
  UNIQUE(group_id, user_id)
);

-- 일일 미션
CREATE TABLE daily_missions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  date DATE NOT NULL,
  
  -- 미션 진행도
  read_posts INTEGER DEFAULT 0,
  write_comments INTEGER DEFAULT 0,
  like_posts INTEGER DEFAULT 0,
  publish_post BOOLEAN DEFAULT FALSE,
  
  -- 보상
  points_earned INTEGER DEFAULT 0,
  completed BOOLEAN DEFAULT FALSE,
  
  created_at TIMESTAMP DEFAULT NOW(),
  
  UNIQUE(user_id, date)
);

CREATE INDEX idx_daily_missions_user_date ON daily_missions(user_id, date);

-- 포인트 거래
CREATE TABLE point_transactions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  
  type VARCHAR(20) NOT NULL, -- earn, spend, purchase
  amount INTEGER NOT NULL,
  reason VARCHAR(100),
  description TEXT,
  
  balance_after INTEGER NOT NULL,
  
  created_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_point_transactions_user ON point_transactions(user_id);

-- 블로거 순위 (캐시 테이블)
CREATE TABLE blogger_rankings (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  
  category VARCHAR(50) NOT NULL,
  
  -- 점수
  blog_index_score FLOAT,
  visitors_score FLOAT,
  content_quality_score FLOAT,
  activity_score FLOAT,
  engagement_score FLOAT,
  total_score FLOAT,
  
  -- 순위
  rank INTEGER,
  prev_rank INTEGER,
  
  -- 기간
  period VARCHAR(20), -- monthly, weekly
  period_date DATE,
  
  created_at TIMESTAMP DEFAULT NOW(),
  
  UNIQUE(user_id, category, period, period_date)
);

CREATE INDEX idx_blogger_rankings_category ON blogger_rankings(category, period, period_date);
```

---

## 9. 컴포넌트 구조

### 9.1 디렉토리 구조

```
src/
├── app/
│   ├── (auth)/
│   │   ├── login/
│   │   └── signup/
│   ├── (dashboard)/
│   │   ├── dashboard/
│   │   ├── ai-writer/
│   │   ├── images/
│   │   ├── community/
│   │   └── analytics/
│   ├── (marketing)/
│   │   ├── page.tsx (홈)
│   │   └── pricing/
│   └── api/
│       ├── auth/
│       ├── ai/
│       ├── keywords/
│       └── community/
│
├── components/
│   ├── ui/ (shadcn/ui)
│   │   ├── button.tsx
│   │   ├── card.tsx
│   │   ├── input.tsx
│   │   └── ...
│   ├── home/
│   │   ├── HeroSection.tsx
│   │   ├── FeaturesSection.tsx
│   │   └── PricingSection.tsx
│   ├── dashboard/
│   │   ├── Sidebar.tsx
│   │   ├── HeroCard.tsx
│   │   └── StatsGrid.tsx
│   ├── ai-writer/
│   │   ├── PersonaSelector.tsx
│   │   ├── ContentSettings.tsx
│   │   ├── GenerationProgress.tsx
│   │   └── ContentEditor.tsx
│   ├── community/
│   │   ├── TodayMissions.tsx
│   │   ├── GroupCard.tsx
│   │   └── Leaderboard.tsx
│   └── shared/
│       ├── Header.tsx
│       ├── Footer.tsx
│       └── LoadingSpinner.tsx
│
├── lib/
│   ├── api/
│   │   ├── auth.ts
│   │   ├── ai.ts
│   │   ├── keywords.ts
│   │   └── community.ts
│   ├── hooks/
│   │   ├── useAuth.ts
│   │   ├── usePoints.ts
│   │   └── useMissions.ts
│   ├── utils/
│   │   ├── cn.ts
│   │   ├── format.ts
│   │   └── validation.ts
│   └── constants/
│       ├── personas.ts
│       └── categories.ts
│
├── types/
│   ├── user.ts
│   ├── persona.ts
│   ├── content.ts
│   └── community.ts
│
└── styles/
    └── globals.css
```

---

## 10. 개발 우선순위

### Phase 1: MVP (1-2개월)

```
Week 1-2: 기본 인프라
├─ Next.js 프로젝트 셋업
├─ TailwindCSS + Shadcn/ui 설정
├─ DB 스키마 생성
├─ 인증 시스템 (이메일/비밀번호)
└─ 기본 레이아웃 (Header, Sidebar, Footer)

Week 3-4: 홈페이지
├─ Hero Section
├─ Features Section
├─ How It Works
├─ Testimonials
├─ Pricing
└─ CTA Section

Week 5-6: 대시보드
├─ 대시보드 메인
├─ Sidebar 네비게이션
├─ 주요 지표 카드
└─ 활동 피드

Week 7-8: AI 블로그 생성기 (핵심)
├─ 페르소나 선택 화면
├─ 콘텐츠 설정 화면
├─ OpenAI API 연동
├─ 생성 프로그레스
├─ 결과 편집기 (Tiptap)
└─ 저장/발행 기능

Week 9-10: 이미지 생성
├─ 나노바나나 API 연동
├─ 이미지 생성 화면
├─ 이미지 라이브러리
└─ 글에 이미지 삽입

Week 11-12: 커뮤니티 기본
├─ 챌린지 그룹 생성
├─ 일일 미션 시스템
├─ 포인트 적립/사용
└─ 리더보드
```

### Phase 2: 고도화 (3-4개월)

```
Month 3:
├─ 키워드 분석 도구
├─ 블로그 지수 조회
├─ 경쟁사 분석 (기본)
└─ 크롬 확장 프로그램 (타이핑 시뮬레이션)

Month 4:
├─ 블로거 순위 시스템
├─ 커뮤니티 고도화 (게이미피케이션)
├─ 예약 발행 시스템
└─ 알림 시스템
```

### Phase 3: 확장 (5-6개월)

```
Month 5-6:
├─ 모바일 최적화
├─ 멘토링 시스템
├─ 고급 분석 대시보드
└─ 결제 시스템 (구독)
```

---

## 📝 개발 체크리스트

### 필수 구현 사항

#### Frontend
- [ ] Next.js 14 프로젝트 초기화
- [ ] TailwindCSS + Shadcn/ui 설정
- [ ] 라우팅 구조 생성
- [ ] 기본 레이아웃 컴포넌트
- [ ] 홈페이지 (마케팅 페이지)
- [ ] 대시보드 레이아웃
- [ ] AI 글쓰기 워크플로우
- [ ] 이미지 생성 화면
- [ ] 커뮤니티 화면
- [ ] 반응형 디자인

#### Backend
- [ ] Express API 서버 셋업
- [ ] PostgreSQL + Prisma ORM
- [ ] JWT 인증 구현
- [ ] OpenAI API 연동
- [ ] 나노바나나 API 연동
- [ ] 키워드 분석 API
- [ ] 커뮤니티 API
- [ ] 파일 업로드 (S3)

#### 기능
- [ ] 회원가입/로그인
- [ ] 페르소나 기반 AI 글쓰기
- [ ] 이미지 생성 (무료)
- [ ] 키워드 검색량 조회
- [ ] 일일 미션 시스템
- [ ] 포인트 경제
- [ ] 리더보드
- [ ] 발행 도우미

---

## 🎯 성공 지표

### 기술 지표
- [ ] Lighthouse 점수 90 이상
- [ ] 페이지 로드 시간 2초 이내
- [ ] SEO 최적화 (메타 태그, sitemap)
- [ ] 반응형 지원 (모바일/태블릿/데스크톱)
- [ ] 크로스 브라우저 테스트 (Chrome, Safari, Firefox)

### 비즈니스 지표
- [ ] 회원가입 퍼널 전환율 60% 이상
- [ ] AI 글쓰기 완료율 80% 이상
- [ ] 일일 미션 참여율 50% 이상
- [ ] 사용자 만족도 4.0/5.0 이상

---

**END OF DOCUMENT**

이 PRD는 개발자가 바로 구현할 수 있도록 작성되었습니다.
화면별 와이어프레임, 컴포넌트 구조, API 명세가 모두 포함되어 있습니다.
